package org.transmartproject.core.multidimensionalquery

/**
 * Constraint for multidimensional data queries
 *
 * No methods, the constraint objects are used internally
 */
interface MultiDimConstraint {
}
