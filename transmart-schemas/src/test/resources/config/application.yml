spring:
    profiles:
        active: test
    datasource:
        platform: h2
        # driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:transmartSchemasTest;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
        username:
        password:
        hikari:
            jdbc-url: jdbc:h2:mem:transmartSchemasTest
